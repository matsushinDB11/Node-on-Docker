version: "3"
services:
  app:
#    build: app/
    image: node:lts
    environment:
      - DEBUG=app:*
      - CHOKIDAR_USEPOLLING=1
    tty: true
    working_dir: /app
    volumes:
      - ./app:/app
    ports:
      - "3000:3000"
#    user: node
    command: npm run dev
